"# 重新架构悬浮窗：单例模式与无缝切换 (Singleton Preview & Seamless Switch)\n\n目前的逻辑是每个卡片“自带”一个独立的悬浮窗，这就导致从卡片 A 移到卡片 B 时，A 必须经历完整的“出场动画”，B 接着经历完整的“进场动画”。\n\n按照您的顶级直觉，最完美的交互应该是：**整个列表共享同一个悬浮窗**。进入列表时悬浮窗出现，在列表内部移动时，悬浮窗不消失，内部的图片像“切牌”一样进行极其丝滑的切换，直到完全离开列表区域，悬浮窗才收起。\n\n## Proposed Changes\n\n### [MODIFY] `premium-interactions.js`\n彻底重写这部分的逻辑，引入**单例模式 (Singleton) 状态机**：\n1. **全局单例**：在 JS 中只生成一个 `<div class=\"work-preview-wrapper\">`，而不是为每个卡片生成一个。\n2. **列表级出入场**：监听 `.work-list` 的 `mouseenter` 和 `mouseleave`。只有在这时，才触发“底层黑卡”的出场和收起动画。\n3. **卡片级图片切换**：监听每个 `.work-card` 的 `mouseenter`。当鼠标移动到新卡片时：\n   - 悬浮窗位置继续平滑跟手。\n   - **核心切换动画**：容器内原有的图片会向上滑出并淡出，新的作品图片会从底部向上滑入并取代它（产生类似于“向上切牌”或“幻灯片上滑”的质感）。\n\n### [MODIFY] `styles.css`\n配合新的 DOM 结构进行微调：\n- 增加图片容器 `.work-preview-img-container`。\n- 确保内部图片的层级和溢出隐藏逻辑，使其支持内部切换。\n\n> [!TIP]\n> 这种全局单例加内部切牌的效果，是 Awwwards 年度网站（SOTY）中最经典的图片序列展示法，物理连贯性直接拉满。\n\n请问这个“切牌”般的图片切换方案，是否完美符合您脑海中的画面？如果同意，我将立即为您重构代码。"