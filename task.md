"# 🃏 悬浮窗“单例模式”升级任务单\n\n- `[x]` **创建全局单例 DOM**\n  - 在 JS 中移除每个卡片单独生成 DOM 的循环，改为在 `.work-list` 外部生成唯一的悬浮窗结构。\n- `[x]` **调整 CSS 遮罩结构**\n  - 新增 `.work-preview-img-container` 以包裹动态生成的图片序列。\n- `[x]` **编写区域级出入场逻辑**\n  - 监听 `.work-list` 的 `mouseenter` 和 `mouseleave` 以控制遮罩和底卡的显隐。\n- `[x]` **编写内部无缝切牌逻辑**\n  - 监听 `.work-card` 的悬停，并在 `img-container` 内部用 GSAP 丝滑切换两张图片（向上滑出/滑入）。\n- `[x]` **编译测试验证**"